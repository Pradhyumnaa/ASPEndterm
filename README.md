# Running the Quizly Application Locally

- `npm install`
- `npm run clean-db` or `npm run clean-db-unix`
- `npm run build-db` or `npm run build-db-unix`
- `npm run start`

# https://quizly-uol-project.site/
